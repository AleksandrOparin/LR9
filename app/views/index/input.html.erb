<!DOCTYPE html>
<html>
  <head>
    <title> Двойные палиндромы </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' %>
  </head>

  <body>
    <%= tag.div class: %w( container d-flex flex-column align-items-center ) do -%>
        <%= form_tag("#", :method => "get", remote: true) do %>
            <%= tag.section class: %w( py-5 text-center container fst-italic ) do -%>
                <%= tag.div class: %w( row py-lg-5 ) do -%>
                    <%= tag.div class: %w( col-lg-6 col-md-8 mx-auto ) do -%>
                        <%= tag.h1 "Ввод числа", class: "fw-light" %>
                        <%= tag.p "Написать программу, определяющую, сколько из чисел в диапазоне от 0 до n (n вводится с клавиатуры), сами являясь палиндромами, при возведении в квадрат, образуют палиндромы.", class: %w( lead text-muted ) %>
                        <%= tag.div class: %w( input-group mb-3 ) do -%>
                            <%= tag.input type: "number", class: "form-control", id: "InputNumber", pattern: "[0-9]+", min: "0", placeholder: "Введите число" %>
                            <%= tag.div class: %w( input-group-append ) do -%>
                                <%= tag.button "Ввод", type: "submit", id: "Result", class: %w( btn btn-outline-primary ) %>
                            <% end -%>
                        <% end -%>

                        <%= tag.table id: "ResultTable", class: %w( table text-center fs-4 ) %>

                        <%= tag.div id: "LengthResult", class: %w( input-group mb-3 ) do -%>
                        <% end -%>
                    <% end -%>
                <% end -%>
            <% end -%>
        <% end %>
    <% end -%>
  </body>
</html>
